#!/bin/bash

# Script لتشغيل التطبيق للنشر على Replit
echo "Starting Al Fursan Quadruple Factory website for deployment..."

# تشغيل Vite preview على المنفذ 8080 المطلوب للنشر
npx vite preview --host 0.0.0.0 --port 8080